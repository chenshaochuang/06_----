I will implement start/stop functionality in the `FloatingWindow` component to allow users to control the timer directly from the floating window.

### Implementation Plan

1.  **Modify `src/renderer/src/components/FloatingWindow.tsx`**:
    *   **Imports**: Add `Play`, `Square` icons from `lucide-react` and `Input` component (if available, or use standard `<input>`).
    *   **State Management**:
        *   Add `inputValue` state to capture the task title when starting a new timer.
    *   **Logic**:
        *   Implement `handleStart`: Creates a new `TimeEntry` with the entered title and calls `window.api.addEntry`.
        *   Implement `handleStop`: Updates the current `activeEntry` with an `endTime` and calls `window.api.updateEntry`.
    *   **UI Updates**:
        *   **Idle State**: Replace the "Idle" text with a compact input field and a "Start" button (Play icon).
        *   **Running State**: Add a "Stop" button (Square icon) next to the timer display.
        *   Ensure the design remains compact and suitable for a floating window.

### Verification
*   **Start Timer**: Verify that entering a title and clicking "Start" in the floating window creates a new active entry and updates the display.
*   **Stop Timer**: Verify that clicking "Stop" in the floating window ends the current task and returns the UI to the idle state.
*   **Sync**: Verify that changes made in the floating window are reflected in the main window (and vice versa, as they share the data source).